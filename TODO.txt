\/ проверить, чтобы проверка денег была у фонда и юзера при донате и пополнении баланса в контроллерах

\/ Везде добавить валидацию проверки параметров

\/ В контроллерах добавить везде приписку к ошибке в defer

\/ Сделать для пользователя в контроллере метод общий donate

Обновление происходит исключительно в сервисах
выполнять донат как транзакцию(либо все либо ничего):
                            Наприме для юзера(
                                вычесть баланс -> 
                                пополнить баланс сбора/фонда -> 
                                создать transaction
                            )
поправить типизацию:
    now -> string json -> float64 в бд и mvc
    then ->int в json и в бд и в mvc

Аутентификация JWT Beared выдать каждой роли свой. Поправить тип токена в responses handlers и controllers

В конструктор AuthService передавать тип Аутентификации(JWT, OAuth и тд)
Валидацию токена засунуть в мидлварь и подумать, что еще передать в валидацию(например роль и операция, чтобы понять,
    соответствует ли данная операция этой роли или нет)


Аутентификация:

Метод 

IMPORTANT:
    Добить авторизацию, а именно метод апи /login прикрутить к контроллеру
    Добавлять токен в заголовки запросов всегда(или это проблема клиента)
    Проверить правильно ли работает настройка роутера
    Добавить, чтобы некоторые поля не отображались при get'ах(например баланс и прочая конфиденциальность)

swagger:
swagger generate spec -m -o=swagger.json
swagger serve -F=swagger swagger.json

integration:

пг дамп миграция

описание тестов для каждой сущности

описать докер

тестконтейнерс

